# Leave VITE_API_BASE_URL empty so axios uses relative URLs.
# Vite dev server proxies /api/* → localhost:5001 (same-origin, cookies work).
# In production, set this to your actual API domain e.g. https://api.yourdomain.com
VITE_API_BASE_URL=
VITE_APP_NAME=SST ERP
